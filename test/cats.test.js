const { assert } = require('chai');
const request = require('./request');
const Cat = require('../lib/models/model');

describe('Cat API', () => {
    it('runs a test', () => {
        assert.ok(true);
    });
});