const { assert } = require('chai');
const request = require('./request');
const Pokemon = require('../lib/models/pokemon');

describe('Pokemen', () => {

    let bulb = {
        name: 'Bulbasaur',
        type: 'Grass'
    };

    let char = {
        name: 'Charmander',
        type: 'Fire'
    };

    it('saves a pokeman', () => {
        
    });

});